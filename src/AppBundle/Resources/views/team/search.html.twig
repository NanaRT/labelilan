{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
				<h1>Rechercher joueurs disponibles pour tournoi</h1>
				<table class="table table-hover">
					<tbody>
			        	{%for team in teams%}
			        		<tr>
			        			<td>
			        				{{team.name}}
			        			</td>
			        			<td align="right">
		          					{%for player in searchCategoryService.getPlayerGame(app.user.id, game.id)%}
    									{% if (is_granted("IS_AUTHENTICATED_REMEMBERED"))
    									and player.team is null %} 
					        				{%if searchCategoryService.checkApplicationTeam(app.user.id, team.id, 'player') == null%}
							                	<a class="btn btn-default" href="{{path('application_new', {'user':app.user.id,'team':team.id,'origin':'player'}) }}" >
							                    	 <span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Recruter
							                    </a>
							                {%else%}
							                	<a class="btn btn-default disabled" href="{{path('application_new', {'user':app.user.id,'team':team.id,'origin':'player'}) }}" >
							                    	 Attente réponse
							                    </a>
							                {%endif%}
					                	{%endif%}
					                {%endfor%}
			        			</td>
			        		</tr>
			        	{%endfor%}
			        </tbody>
				</table>
			</div>
        </div>
	</div>
{% endblock %}
