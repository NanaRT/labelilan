{% extends '::base.html.twig' %}

{% block body %}
	{%if (is_granted("ROLE_ADMIN"))%}
	    <div class="container">
	        <div class="row">
			    <h1>Pizza creation</h1>
			
			    {{ form(form) }}
			
			        <ul class="record_actions">
			    <li>
			        <a href="{{ path('pizza') }}">
			            Back to the list
			        </a>
			    </li>
			</ul>
			</div>
		</div>
	{%endif%}
{% endblock %}
			
