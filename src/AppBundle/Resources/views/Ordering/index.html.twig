{% extends '::base.html.twig' %}

{% block body -%}
	{%if (is_granted("ROLE_ADMIN"))%}
	    <div class="container">
	        <div class="row">
			    <h1>Ordering list</h1>
			
			    <table  class="table table-hover">
			        <thead>
			            <tr>
			                <th>Id</th>
			                <th>Number</th>
			                <th>Actions</th>
			            </tr>
			        </thead>
			        <tbody>
			        {% for entity in entities %}
			            <tr>
			                <td><a href="{{ path('ordering_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
			                <td>{{ entity.number }}</td>
			                <td>
			                <ul>
			                    <li>
			                        <a href="{{ path('ordering_show', { 'id': entity.id }) }}">show</a>
			                    </li>
			                    <li>
			                        <a href="{{ path('ordering_edit', { 'id': entity.id }) }}">edit</a>
			                    </li>
			                </ul>
			                </td>
			            </tr>
			        {% endfor %}
			        </tbody>
			    </table>
			
			        <ul>
			        <li>
			            <a href="{{ path('ordering_new') }}">
			                Create a new entry
			            </a>
			        </li>
			    </ul>
			</div>
		</div>
	{%endif%}
{% endblock %}
