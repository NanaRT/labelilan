AppBundle\Entity\User:
    type:  entity
    table: fos_user
    id:
        id:
            type: integer
            generator:
                strategy: AUTO
    
    oneToMany:
        player:
            targetEntity: Player
            mappedBy: user
        application:
            targetEntity: Application
            mappedBy: user
